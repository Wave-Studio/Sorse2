/**
 * Sorse 2
 *
 * Developed by Wave-studio
 */
import { ShapeCore } from "../../../../index"

export class Rectangle extends ShapeCore {
	// TODO: Add rest of stuff
	constructor(position: { x: number, y: number }) {
		super(position.x, position.y);
	}
}