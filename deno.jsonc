{
	"lint": {
		"files": {
			"exclude": [
				"node_modules",
				// Wildcards don't seem to work
				"apps/docs/node_modules",
				"apps/docs/dist",
				"apps/sorse-example/node_modules",
				"apps/sorse-example/dist",
				"packages/config/node_modules"
			]
		},
		"rules": {
			// Doesn't work for some reason
			"exclude": ["no-local"]
		}
	},
	"compilerOptions": {
		"lib": ["deno.window", "ESNext", "DOM"]
	}
}
